# CLAUDE.md

의존성 없는 단일 HTML 미니 앱 모음. 각 앱은 `apps/<name>/index.html` 하나로 완결됩니다.

## Agent skills

### Issue tracker

이슈와 스펙은 `.scratch/<feature>/` 아래 마크다운 파일로 관리합니다. See `docs/agents/issue-tracker.md`.

### Triage labels

다섯 개의 표준 트리아지 라벨을 그대로 사용합니다. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: 루트의 `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
